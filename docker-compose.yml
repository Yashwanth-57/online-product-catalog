version: "3.9"

services:
  nextjs-app:
    build: .
    container_name: my-product-app
    ports:
      - "3030:3000"   # host:container
    environment:
      - NODE_ENV=production
    env_file:
      - .env
    restart: always

